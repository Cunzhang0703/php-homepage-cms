# 安全策略

## 支持的版本

仅对最新发布版本提供安全修复。当前支持版本：**V1.26**。

| 版本 | 支持状态 |
| --- | --- |
| V1.26 | ✅ 支持 |
| ≤ V1.25 | ❌ 不再支持，请先升级 |

升级前请务必在后台「备份恢复」创建一次备份。

---

## 上报安全漏洞

**请勿通过公开 Issue、Discussion 或 PR 上报安全问题**，那会在他人在修复前暴露风险。

请使用 GitHub 的**私密漏洞上报**（内容不公开，仅维护者可见）：

> https://github.com/Cunzhang0703/php-homepage-cms/security/advisories/new

若该入口暂不可用，可先开一个**不含任何细节**的 Issue 说明"有安全问题需要私下沟通"，
维护者会另行提供渠道。

上报内容请包含：

- 漏洞类型（例如 SQL 注入、越权、文件上传绕过、XSS、SSRF）
- 受影响的版本与文件路径
- 复现步骤或概念验证（PoC）
- 影响范围评估（是否需要登录、能否未授权触发）
- 你希望的署名方式（如愿意被致谢）

**响应时限**（尽力而为，非商业承诺）：

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到 | 3 个工作日内 |
| 初步评估与定级 | 7 个工作日内 |
| 修复与发布 | 依严重程度协商确定 |

在修复版本发布前，请对细节保密。修复发布后，如果你愿意，我们会在 `CHANGELOG.md` 中致谢。

---

## 安全边界与已知前提

本项目在**应用层**做了以下防护（详见 [docs/deployment-security.md](docs/deployment-security.md)）：

- 后台 CSRF Token、防暴力登录、签名会话令牌
- Cookie 具备 `HttpOnly` / `SameSite=Lax`，HTTPS 下自动 `Secure`
- 登录账号与密码区分大小写
- 升级包强制 64 位 SHA256 校验，拒绝 `../` 与符号链接
- 源码包上传后自动剔除 `includes/db.php`、`data/`、`uploads/`、`install/`
- 公开接口限流与来源校验

但以下事项**属于部署方的责任**，不在本项目代码防护范围内。若因此产生问题，不会被判定为项目漏洞：

1. **未启用 HTTPS**，或未将 HTTP 跳转到 HTTPS
2. **未按文档封锁敏感目录**：`data/`、`includes/`、`install/`，以及以点号开头的文件
3. **安装完成后未删除或禁用 `install/` 目录**，导致可被重新初始化
4. **未限制 `uploads/` 的脚本执行权限**
5. **`includes/db.php` 泄露**（例如误提交到公开仓库、备份文件暴露在外）
6. **服务器、PHP 版本或数据库本身存在已知漏洞**且未打补丁
7. **弱口令**：管理员密码过于简单，或被用于其他站点后遭撞库

---

## 自检清单

上线前请逐项确认 [tests/README.md](tests/README.md) 中的「1. 后台安全」与「2. 文件与更新」两节，并完成 [docs/deployment-security.md](docs/deployment-security.md) 的全部「必须完成」项。
